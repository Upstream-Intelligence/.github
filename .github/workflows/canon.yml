name: canon

on:
  push:
  pull_request:

jobs:
  canon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No retired Pioneer/pricing/pack framing on the public profile
        run: bash scripts/check-canon.sh
